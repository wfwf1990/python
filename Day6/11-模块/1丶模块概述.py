'''
概述：
    目前代码比较少,写在一个文件中还体现不出什么缺点,但是随着代码量越来越多,代码就越来越难维护
    为了解决难以维护的问题,我们把很多相似功能的函数分组，放在不同的文件中取,这样每个文件所包含的内容相对较少,而且对于每一个文件的大致
    功能可用文件名来体现,很多编程语言都是这么来组织代码结构.一个.py文件就是一个模块

优点：
    1丶提高代码的可维护性
    2丶提高了代码的维护性，当一个模块完毕,可以被多个地方引用
    3丶引用其他的模块（内置模块丶三方模块和自定义模块）
    4丶避免函数名和变量名的冲突
'''
